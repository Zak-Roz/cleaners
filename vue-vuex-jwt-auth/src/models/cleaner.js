export default class Cleaner {
  constructor(title, description, services, images) {
    this.title = title;
    this.description = description;
    this.services = services;
    this.images = images;
  }
}
